# Frontend (Next.js)

Prefer the repo root `README.md` for full end-to-end setup (backend + DB + extension).

## Run (dev)

```bash
npm install
npm run dev
```

## Build (prod)

```bash
npm run lint
npm run build
npm start
```
